{
  "name": "bontrip",
  "version": "1.0.0",
  "author": "Haydee Munoz",
  "license": "ISC",
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"node server.js\" \"npm client\"",
    "heroku-postbuild": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4"
  },
  "engines": {
    "node": "10.3.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
